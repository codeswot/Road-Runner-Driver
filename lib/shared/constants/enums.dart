enum UserType {
  customer,
  driver,
}

enum OrderStatus {
  arrived,
  waitingDriver,
  packageEnroute,
  dropOff,
  cancelled,
}

enum VehicleType {
  motorcycle,
  triCycle,
  car,
  miniVan,
  truck,
}
